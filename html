<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Docs — My Project</title>

<!-- Google Font -->
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&display=swap" rel="stylesheet">

<!-- Prism for code highlighting (optional) -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/themes/prism-tomorrow.min.css" rel="stylesheet" />
<script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/prism.min.js" defer></script>

<style>
  :root{
    --bg: #0f1724;
    --panel: #0b1220;
    --muted: #9aa4b2;
    --text: #e6eef6;
    --accent: #6ee7b7;
    --glass: rgba(255,255,255,0.03);
    --radius: 12px;
    --shadow: 0 6px 24px rgba(2,6,23,0.6);
    --max-width: 1100px;
    --sidebar-w: 260px;
    font-family: "Inter", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
  }

  /* Light theme */
  .light {
    --bg: #f7fafc;
    --panel: #ffffff;
    --muted: #6b7280;
    --text: #0b1220;
    --accent: #0ea5a4;
    --glass: rgba(2,6,23,0.03);
    --shadow: 0 8px 30px rgba(15,23,42,0.06);
  }

  * {box-sizing: border-box}
  html,body{height:100%;margin:0;background:linear-gradient(180deg,var(--bg),rgba(0,0,0,0.02));color:var(--text);-webkit-font-smoothing:antialiased}
  a{color:var(--accent);text-decoration:none}
  .app {
    display:flex;
    min-height:100vh;
    gap:28px;
    padding:28px;
    align-items:flex-start;
    justify-content:center;
  }

  /* Sidebar */
  .sidebar {
    width:var(--sidebar-w);
    min-height: calc(100vh - 56px);
    background: linear-gradient(180deg, rgba(255,255,255,0.02), transparent);
    border-radius:var(--radius);
    padding:18px;
    box-shadow:var(--shadow);
    position:sticky;
    top:18px;
    flex-shrink:0;
    backdrop-filter: blur(6px);
    border: 1px solid rgba(255,255,255,0.03);
  }
  .brand {display:flex;gap:10px;align-items:center;margin-bottom:18px}
  .logo {
    width:44px;height:44px;border-radius:10px;background:linear-gradient(135deg,var(--accent),#60a5fa);
    display:grid;place-items:center;font-weight:800;color:var(--panel);
    box-shadow: 0 6px 18px rgba(11,19,30,0.5);
  }
  .brand h1{font-size:15px;margin:0}
  .brand p{margin:0;font-size:12px;color:var(--muted)}

  .nav {margin-top:8px}
  .nav a{
    display:block;padding:10px 12px;border-radius:10px;color:var(--text);margin-bottom:6px;
    font-weight:600;font-size:14px;
  }
  .nav a:hover{background:var(--glass);transform:translateY(-1px)}
  .nav a.active{background:linear-gradient(90deg, rgba(255,255,255,0.03), rgba(255,255,255,0.01));box-shadow: inset 0 0 0 1px rgba(255,255,255,0.01)}

  .sidebar-footer{margin-top:auto;padding-top:12px;border-top:1px dashed rgba(255,255,255,0.02);font-size:13px;color:var(--muted)}

  /* Main content */
  .content-wrap{max-width:var(--max-width);width:100%}
  .topbar{
    display:flex;gap:12px;align-items:center;margin-bottom:18px;
  }
  .search {
    flex:1;display:flex;align-items:center;background:var(--panel);padding:10px 12px;border-radius:12px;border:1px solid rgba(255,255,255,0.02);
    box-shadow: 0 6px 18px rgba(2,6,23,0.4);
  }
  .search input{flex:1;background:transparent;border:0;color:var(--muted);outline:none;font-weight:600}
  .btn {
    display:inline-flex;align-items:center;gap:8px;padding:10px 12px;border-radius:10px;background:linear-gradient(90deg,var(--accent),#60a5fa);color:var(--panel);font-weight:700;border:0;cursor:pointer;
    box-shadow: 0 8px 20px rgba(14,167,164,0.12);
  }
  .theme-toggle{padding:8px;border-radius:10px;background:transparent;border:1px solid rgba(255,255,255,0.03);cursor:pointer;color:var(--muted)}

  .doc {
    background:linear-gradient(180deg, rgba(255,255,255,0.02), transparent);
    padding:28px;border-radius:16px;box-shadow:var(--shadow);border:1px solid rgba(255,255,255,0.03);
  }
  .doc h2{margin-top:0}
  .meta {display:flex;gap:10px;align-items:center;color:var(--muted);font-size:14px;margin-bottom:12px}
  .lead{color:var(--muted);font-size:15px;margin-bottom:18px}

  /* Floating TOC */
  .toc {
    position:sticky; top:110px; margin-left:18px;max-width:220px;
    font-size:14px;color:var(--muted);
  }
  .toc a{display:block;padding:6px 8px;border-radius:8px}
  .toc a:hover{background:var(--glass);color:var(--text)}

  /* Code block niceties */
  pre {border-radius:10px;padding:14px;overflow:auto;margin:14px 0}
  code[class*="language-"]{font-size:13px}

  /* Headings with link button */
  .heading {
    display:flex;align-items:center;gap:8px;
  }
  .anchor {
    opacity:0;color:var(--muted);font-size:13px;padding:6px;border-radius:8px;border:1px solid transparent;
  }
  .heading:hover .anchor{opacity:1}

  /* Responsive */
  @media (max-width: 980px){
    .app{padding:18px}
    .sidebar{display:none}
    .topbar{flex-direction:row;gap:8px}
  }
  @media (max-width: 640px){
    .topbar{flex-direction:column;align-items:stretch}
    .search{padding:10px}
    .btn{width:100%}
  }
</style>
</head>
<body>
<div id="root" class="app">

  <!-- SIDEBAR -->
  <aside class="sidebar" aria-label="Documentation sidebar">
    <div class="brand">
      <div class="logo">DN</div>
      <div>
        <h1>DocNest</h1>
        <p>Developer docs • v1.0</p>
      </div>
    </div>

    <nav class="nav" id="sidebar-nav">
      <a href="#intro" class="active">Introduction</a>
      <a href="#getting-started">Getting Started</a>
      <a href="#usage">Usage</a>
      <a href="#api">API</a>
      <a href="#components">Components</a>
      <a href="#faq">FAQ</a>
    </nav>

    <div class="sidebar-footer">
      <div style="display:flex;justify-content:space-between;align-items:center">
        <small>Last updated</small>
        <small>Oct 7, 2025</small>
      </div>
      <div style="margin-top:8px;font-size:13px">Made with ♥ — Customize freely</div>
    </div>
  </aside>

  <!-- MAIN AREA -->
  <main class="content-wrap">
    <div class="topbar">
      <div class="search" role="search" aria-label="Search docs">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" style="opacity:.7"><path d="M21 21l-4.35-4.35" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/><circle cx="11" cy="11" r="6" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>
        <input id="search-input" placeholder="Search docs (try 'API', 'components'...)" />
      </div>

      <button class="theme-toggle" id="theme-toggle" title="Toggle theme">Toggle</button>
      <button class="btn" id="open-search" title="Quick actions">Quick Action</button>
    </div>

    <div style="display:flex;gap:18px;align-items:flex-start">
      <article class="doc" id="doc">
        <div class="meta">
          <strong>Docs</strong>
          <div style="margin-left:8px">•</div>
          <div>v1.0 • Stable</div>
        </div>

        <h2 id="intro" class="heading">
          <span>Introduction</span>
          <a class="anchor" data-href="#intro" title="Copy link" onclick="copyAnchor(this)">🔗</a>
        </h2>

        <p class="lead">A sleek, readable docs template — minimal, fast, and responsive. Use it for libraries, components, or internal knowledgebases.</p>

        <h3 id="getting-started" class="heading">
          Getting started
          <a class="anchor" data-href="#getting-started" title="Copy link" onclick="copyAnchor(this)">🔗</a>
        </h3>
        <p>Drop this file into your repo. Edit the sidebar nav, add pages, or split into templates as you need.</p>

        <pre><code class="language-html">&lt;!-- Example install snippet --&gt;
&lt;script src="https://cdn.example.com/your-lib.min.js"&gt;&lt;/script&gt;</code></pre>

        <h3 id="usage" class="heading">
          Usage
          <a class="anchor" data-href="#usage" title="Copy link" onclick="copyAnchor(this)">🔗</a>
        </h3>
        <p>Show documentation sections, API descriptions, and code samples. Keep examples small and focused for readability.</p>

        <h3 id="api" class="heading">
          API
          <a class="anchor" data-href="#api" title="Copy link" onclick="copyAnchor(this)">🔗</a>
        </h3>
        <p>Here is a sample function signature:</p>

        <pre><code class="language-js">// greet(name: string) => string
function greet(name){
  return `Hello, ${name}!`;
}</code></pre>

        <h3 id="components" class="heading">
          Components
          <a class="anchor" data-href="#components" title="Copy link" onclick="copyAnchor(this)">🔗</a>
        </h3>
        <p>Document UI components with props and small interactive examples.</p>

        <h3 id="faq" class="heading">
          FAQ
          <a class="anchor" data-href="#faq" title="Copy link" onclick="copyAnchor(this)">🔗</a>
        </h3>
        <p>If you need more, add a "Contributing" section and a shorter CHANGELOG page.</p>

        <hr style="border:none;height:1px;background:linear-gradient(90deg,transparent,var(--glass),transparent);margin:18px 0">

        <p style="font-size:13px;color:var(--muted)">Pro tip: expose the sidebar nav as a reusable component and load content via Markdown -> HTML pipeline for maintainability.</p>
      </article>

      <!-- Table of contents / aside -->
      <aside class="toc" aria-label="Table of contents">
        <strong style="display:block;margin-bottom:8px">On this page</strong>
        <nav>
          <a href="#intro">Introduction</a>
          <a href="#getting-started">Getting started</a>
          <a href="#usage">Usage</a>
          <a href="#api">API</a>
          <a href="#components">Components</a>
          <a href="#faq">FAQ</a>
        </nav>
      </aside>
    </div>
  </main>
</div>

<script>
  // Theme toggle with localStorage
  (function(){
    const root = document.documentElement;
    const themeToggle = document.getElementById('theme-toggle');
    const stored = localStorage.getItem('docs-theme');
    if(stored === 'light') document.body.classList.add('light');

    themeToggle.addEventListener('click', () => {
      document.body.classList.toggle('light');
      localStorage.setItem('docs-theme', document.body.classList.contains('light') ? 'light' : 'dark');
      themeToggle.innerText = document.body.classList.contains('light') ? '🌞 Light' : '🌙 Dark';
    });
    themeToggle.innerText = document.body.classList.contains('light') ? '🌞 Light' : '🌙 Dark';
  })();

  // Sidebar nav active state observer & smooth scrolling
  (function(){
    const links = Array.from(document.querySelectorAll('#sidebar-nav a'));
    function setActive(hash){
      links.forEach(a => a.classList.toggle('active', a.getAttribute('href') === hash));
    }
    // click -> smooth scroll
    links.forEach(a => a.addEventListener('click', (e)=>{
      e.preventDefault();
      const id = a.getAttribute('href').slice(1);
      document.getElementById(id).scrollIntoView({behavior:'smooth',block:'start'});
      history.replaceState(null, '', '#' + id);
      setActive('#' + id);
    }));
    // update on load based on hash
    if(location.hash) setActive(location.hash);
  })();

  // Copy anchor link
  function copyAnchor(el){
    const href = el.dataset.href || '#' + (el.parentElement.getAttribute('id') || el.parentElement.querySelector('h2,h3')?.id);
    const url = location.origin + location.pathname + href;
    navigator.clipboard?.writeText(url).then(()=>{
      el.innerText = '✓';
      setTimeout(()=> el.innerText = '🔗', 900);
    }).catch(()=> {
      // fallback: select and alert
      alert('Copy this link: ' + url);
    });
  }

  // Simple search: filter sidebar items and content headings
  (function(){
    const input = document.getElementById('search-input');
    const navLinks = document.querySelectorAll('#sidebar-nav a');
    input.addEventListener('input', () => {
      const q = input.value.trim().toLowerCase();
      if(!q){
        navLinks.forEach(a => a.style.display = '');
        return;
      }
      navLinks.forEach(a => {
        const text = a.textContent.toLowerCase();
        a.style.display = text.includes(q) ? '' : 'none';
      });
    });
  })();
</script>
</body>
</html>
